# AI-Sales-Forecasting
An AI model helps predict sales.  It is built using Python and Scikit-Learn.  The model uses past data to make predictions.  This data is put into the model through a Pandas system that works automatically.  The goal is to guess sales for the 6 months. The model also uses Matplotlib and Seaborn to show sales trends.  
